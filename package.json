{
  "name": "library-management-system",
  "version": "0.1.0",
  "private": true,
  "description": "Local Hebrew RTL library management system",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.18.0"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently --names \"api,web\" --prefix-colors \"green,blue\" \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
    "test": "npm run test --prefix backend --if-present && npm run test --prefix frontend --if-present",
    "clean": "npm run clean --prefix backend && npm run clean --prefix frontend",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "db:generate": "npm run db:generate --prefix backend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:push": "npm run db:push --prefix backend",
    "db:reset": "npm run db:reset --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "db:studio": "npm run db:studio --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "prettier": "^3.9.6"
  }
}
